@page
@model DiamondStore.Pages.Admin.AddDiamondModel
@{
    Layout = "_LayoutAdmin";
    ViewData["Title"] = "Add Diamond";
}

<main class="h-full pb-16 overflow-y-auto">
    <div class="container grid px-6 mx-auto">
        <h2 class="my-6 text-2xl font-semibold text-gray-700 dark:text-gray-200">
            Add Diamond
        </h2>
        <form method="post">
            <div class="mb-4">
                <label class="block text-sm">Diamond Name</label>
                <input type="text" name="Diamond.DiamondName" class="form-input mt-1 block w-full" />
            </div>
            <div class="mb-4">
                <label class="block text-sm">Price</label>
                <input type="number" name="Diamond.DiamondPrice" class="form-input mt-1 block w-full" />
            </div>
            <div class="mb-4">
                <label class="block text-sm">Weight</label>
                <input type="number" name="Diamond.DiamondWeight" class="form-input mt-1 block w-full" />
            </div>
            <div class="mb-4">
                <label class="block text-sm">Color</label>
                <select name="Diamond.DiamondColorId" class="form-select mt-1 block w-full">
                    @foreach (var color in Model.Colors)
                    {
                        <option value="@color.Id">@color.Name</option>
                    }
                </select>
            </div>
            <div class="mb-4">
                <label class="block text-sm">Clarity</label>
                <select name="Diamond.DiamondClarityId" class="form-select mt-1 block w-full">
                    @foreach (var clarity in Model.Clarities)
                    {
                        <option value="@clarity.Id">@clarity.Name</option>
                    }
                </select>
            </div>
            <div class="mb-4">
                <label class="block text-sm">Cut</label>
                <select name="Diamond.DiamondCutId" class="form-select mt-1 block w-full">
                    @foreach (var cut in Model.Cuts)
                    {
                        <option value="@cut.Id">@cut.Name</option>
                    }
                </select>
            </div>
            <div class="mb-4">
                <button type="submit" class="btn btn-primary">Add Diamond</button>
            </div>
        </form>
    </div>
</main>
